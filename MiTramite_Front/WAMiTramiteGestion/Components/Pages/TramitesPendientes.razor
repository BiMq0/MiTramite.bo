@page "/tramites-pendientes"

<div class="dashboard-container">
    <!-- Sidebar -->
    <aside class="sidebar shadow">
        <div class="sidebar-header text-center mb-4">
            <h3 class="fw-bold text-light">Funcionario</h3>
            <hr class="bg-light opacity-25">
        </div>

        <nav class="nav flex-column">
            <NavLink class="nav-link" href="/dashboardfuncionario">🏠 Inicio</NavLink>
            <NavLink class="nav-link active" href="/tramites-pendientes">📄 Trámites Pendientes</NavLink>
            <NavLink class="nav-link" href="/reportes">📊 Reportes</NavLink>
            <NavLink class="nav-link" href="/configuracion">⚙️ Configuración</NavLink>
            <NavLink class="nav-link text-danger mt-auto" href="/">🚪 Cerrar Sesión</NavLink>
        </nav>
    </aside>

    <!-- Contenido -->
    <main class="content">
        <div class="container-fluid px-4">
            <h2 class="fw-bold text-primary mb-4 mt-3">📄 Trámites Pendientes</h2>

            <div class="card border-0 shadow p-4 animate-fadein">
                <p>Listado de trámites que están actualmente pendientes de revisión o aprobación.</p>

                <div class="table-wrapper mt-3">
                    <table class="custom-table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Tipo de Trámite</th>
                                <th>Solicitante</th>
                                <th>Fecha</th>
                                <th>Estado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>1</td>
                                <td>Actualización de Datos</td>
                                <td>Lucía Ramos</td>
                                <td>2025-10-25</td>
                                <td><span class="badge bg-warning text-dark">Pendiente</span></td>
                                <td><button class="btn btn-sm btn-gradient">Revisar</button></td>
                            </tr>
                            <tr>
                                <td>2</td>
                                <td>Afiliación</td>
                                <td>José Fernández</td>
                                <td>2025-10-26</td>
                                <td><span class="badge bg-warning text-dark">Pendiente</span></td>
                                <td><button class="btn btn-sm btn-gradient">Revisar</button></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
</div>

<style>
    @@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap');

    .dashboard-container {
        display: flex;
        min-height: 100vh;
        background-color: #f8f9fa;
        font-family: 'Inter', sans-serif;
    }

    .sidebar {
        width: 250px;
        background: linear-gradient(180deg, #1a365d, #2c5282);
        color: white;
        display: flex;
        flex-direction: column;
        padding: 1.5rem 1rem;
    }

    .nav-link {
        color: #ffffffcc;
        padding: 0.75rem 1rem;
        border-radius: 0.5rem;
        margin-bottom: 0.5rem;
        transition: all 0.2s ease;
        font-weight: 500;
    }

        .nav-link:hover {
            background: rgba(255,255,255,0.2);
            transform: translateX(5px);
            color: #fff;
        }

        .nav-link.active {
            background: rgba(255,255,255,0.25);
            font-weight: 600;
            color: #fff;
        }

    .content {
        flex-grow: 1;
        padding: 2rem;
        background-color: #f5f7fb;
    }

    .card {
        border-radius: 1rem;
        background: #fff;
    }

    .custom-table {
        width: 100%;
        border-collapse: collapse;
        background: white;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.05);
    }

        .custom-table thead {
            background: linear-gradient(135deg, #1a365d, #2c5282);
            color: white;
            font-weight: 600;
        }

        .custom-table th, .custom-table td {
            padding: 12px 18px;
            border-bottom: 1px solid #e9ecef;
        }

        .custom-table tbody tr:hover {
            background-color: #f1f7ff;
        }

    .btn-gradient {
        background: linear-gradient(135deg, #1a365d, #3182ce);
        border: none;
        color: white;
        transition: 0.3s;
    }

        .btn-gradient:hover {
            background: linear-gradient(135deg, #0f2027, #1a365d);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(26,54,93,0.4);
        }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fadein {
        animation: fadeIn 0.8s ease-in-out;
    }

    @@media (max-width: 768px) {
        .sidebar {
            display: none;
        }

        .content {
            padding: 1rem;
        }
    }
</style>
